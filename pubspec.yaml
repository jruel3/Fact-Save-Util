name: fact_game_copy
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.0

dependencies:
  ffi: ^2.1.0
  fluent_ui: ^4.8.0
  system_theme: ^2.3.1
  window_manager: ^0.3.9
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
